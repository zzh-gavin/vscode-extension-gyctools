//------------------------------------------------------------------------------
// 
// this file is generated by gystools from the template named '/cscharp/object.njk'. 
// 
// author: {{ customsAttributes.author }}
//------------------------------------------------------------------------------
{% for import in importArray %}
using {{ import }};
{% endfor %}
//Add manual codes in patella holding range , they will be reserved when anthour generation.-->
//patella:holding codes name=usings
{{ holdingCode.usings }}
//patella:end holding codes


namespace {{ customsAttributes.cSharpObjectNameSpace }}
{
    public class {{className}}{
        {% for property in properties %}
        /// <summary>
        /// {{ property.comment }}
        /// </summary>
        public {{ property.propertyType }} {{ property.propertyName }} { get; set; }
        {% endfor %}

        //Add manual codes in patella holding range , they will be reserved when anthour generation.
        //patella:holding codes name=contents
        {{ holdingCode.contents }}
        //patella:end holding codes
        
    }
}